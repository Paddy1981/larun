# Minimal requirements for Railway API deployment
# This file installs ONLY what's needed to run the FastAPI server with TinyML models

# FastAPI & Server
fastapi>=0.109.0
uvicorn[standard]>=0.27.0
python-multipart>=0.0.6

# TensorFlow Lite (lightweight, 10x smaller than TensorFlow)
tflite-runtime>=2.14.0; platform_machine != 'x86_64'
tensorflow>=2.13.0,<2.16.0; platform_machine == 'x86_64'

# Core Data Processing (minimal)
numpy>=1.24.0,<2.0.0
pydantic>=2.5.0

# Database & Cloud
supabase>=2.3.0
python-dotenv>=1.0.0

# Configuration
pyyaml>=6.0

# HTTP Client
aiohttp>=3.9.0
requests>=2.31.0

# CORS & Middleware
python-jose[cryptography]>=3.3.0
